<header>
    <div class="collapse bg-dark" id="navbarHeader">
        <div class="container">
            <div class="row">
                <div class="col-sm-8 col-md-7 py-4">
                    <h4 class="text-white">Über mich</h4>
                    <div class="text-muted">
                        {% set widget = widget.getWidget('ueber_mich') %}
                        {{ widget_translation.getWidgetTranslation(widget, page.getLanguage()).getContent()|raw }}
                    </div>
                </div>
                <div class="col-sm-4 offset-md-1 py-4 contact">
                    <h4 class="text-white">Kontakt</h4>
                    {% set widget = widget.getWidget('kontakt') %}
                    {{ widget_translation.getWidgetTranslation(widget, page.getLanguage()).getContent()|raw }}
                </div>
            </div>
        </div>
    </div>
    <div class="navbar navbar-dark bg-dark shadow-sm">
        <div class="container d-flex justify-content-between">
            <a href="{{ base_url() }}" class="navbar-brand d-flex align-items-center">
                <strong>Zum Shop</strong>
            </a>
            <button class="navbar-toggler px-3 py-2" type="button" data-toggle="collapse" data-target="#navbarHeader" aria-controls="navbarHeader" aria-expanded="false" aria-label="Toggle navigation">
                <i class="fa fa-bars"></i>
            </button>
        </div>
    </div>
</header>
